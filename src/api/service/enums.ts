export enum LoggerTypeEnum {
  SUCCESS = 'success',
  FAILURE = 'failure',
  INFO = 'info',
  DEFAULT = 'default',
}

export enum LoggerServiceEnum {
  AXIOS = 'axios',
  REACT_QUERY = 'react-query',
  DEFAULT = 'Message',
}
